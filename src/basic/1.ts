// Є наступний JavaScript код:
// const age = 50;
// const username = 'Max';
// const toggle = true;
// const empty = null;
// const callback = (a) => { return 100 + a };

const age: number = 50; // число.
const username: string = "Max"; // рядок.
const toggle: boolean = true; // логічне.
const empty: null = null; // null.
const callback: (a: number) => number = (a) => {
  return 100 + a;
};
